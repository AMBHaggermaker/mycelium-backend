Build four profile and maker experience improvements in one pass.
FEATURE ONE — Fix sticker mobile scaling. Stickers on profile pages use fixed pixel positions set on desktop that overlap text and UI elements on mobile screens. Fix sticker positioning to use percentage-based coordinates relative to the profile container width and height so stickers scale proportionally across all screen sizes. Add minimum and maximum size constraints so stickers don't become too large on mobile or too small on desktop. On mobile screens under 768px render stickers at 60% of their desktop size. Also fix the avatar upload area — the photo size recommendation text that says 400x square display as circle is showing overlaid on top of the avatar image on mobile in normal view mode. Hide this text completely in normal view and only show it when the user is actively in Edit Profile mode.
FEATURE TWO — Maker profile page customization. Add the same customization system that profile pages have to Maker profile pages. Maker profiles at /makers/:username get: a banner image upload with crop tool at 1200x400px 3:1 ratio, a background color picker or pattern selector (same CSS patterns as profiles — solid, stripes, grid, dots, checkerboard, zigzag, diamonds, honeycomb, crosshatch, waves, triangles, stars, Mycelium branching pattern), an accent color picker that controls the red glow color on their maker page (defaults to the platform red #ff3366 but makers can change it to their own brand color), a font style selector (Mystical/Modern/Classic/Typewriter). Add an Edit Maker Page button visible only to the maker on their own page. Store customization in a maker_page_settings jsonb column on the maker_profiles table. Apply settings as CSS custom property overrides on the maker page. Banner image uploads to R2 using existing infrastructure.
FEATURE THREE — Business page customization. Add the same customization to business pages at /businesses/:id. Business pages get: banner image upload with crop tool at 1200x400px, background color or pattern selector, accent color picker (defaults to platform green #00ff88), font style selector. Add an Edit Business Page button visible only to the business owner. Store in a business_page_settings jsonb column on the businesses table. Apply as CSS custom property overrides.
FEATURE FOUR — PWA icon cache busting and transparency panels. First — fix the PWA icon still showing as green square on phones. Update the service worker to version mycelium-v4, add cache busting to the manifest icon URLs with ?v=3 query strings, and add a Cache-Control no-cache header to the manifest.json nginx config so browsers always fetch fresh manifest. Second — add Why This Works This Way transparency panels to key sections of the platform. Each panel is a small collapsible info section with a mycelium branch icon that expands when clicked. Add these panels: On the Hotlight feed page — Why no likes or reactions? Because we want dialogue not dopamine. A reply requires a thought. A like requires nothing and tells you nothing. On the Maker's Guild page — Why these metrics? These numbers show what actually matters to you as a creator — not what keeps you posting more. We show plays, completions, return listeners, and commissions. We never show how you compare to others or how to game an algorithm. On the Watch dashboards — Why community submitted reports? Because the people closest to a problem see it first. Institutions report what they are required to report. Communities report what they actually experience. On the invitation system page — Why invitation only? Because anonymous platforms become what anonymous people do to each other. Real identity and human vouching changes what people are willing to say and do. On the Covenant page — Why a Covenant instead of terms of service? Because terms of service protect the platform from users. A Covenant is a mutual commitment between the platform and the community. Style the panels as small collapsible sections with a subtle neon green glow border, a mycelium branch SVG icon, and the header text Why this works this way in Cinzel Decorative font. The expanded content uses the platform body font in muted sage color. Collapsed by default, remembers open/closed state in localStorage per panel.
Then rebuild and redeploy the frontend and push to GitHub.