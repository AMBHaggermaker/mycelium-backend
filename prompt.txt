
Build four profile enhancement features in one pass.
FEATURE ONE — Fix banner change button. The Change Banner button disappears after a banner image is uploaded. Fix it so the button is always visible to the profile owner whether or not a banner exists. Add it as a persistent small camera icon button in the top right corner of the banner area with a semi-transparent dark background so it's visible over any banner color or image. Clicking it opens the existing banner upload flow with the crop tool.
FEATURE TWO — Full page background photo. Add a background photo option to the profile page. Add a Set Background button in Edit Profile mode. The background photo uploads to R2 using the existing crop tool with a 16:9 ratio at 1920x1080px. The photo displays behind all the board cards as a full page background. Add a background overlay opacity slider in the Appearance tab of Edit Profile — slider goes from 0 percent (no overlay, raw photo) to 90 percent (nearly solid color). The overlay color matches the profile accent color. On mobile the background scales and crops to fill the screen without breaking the board layout. Store background_photo_url and background_overlay_opacity in the profiles table. Falls back to solid background color if no background photo is set.
FEATURE THREE — Left sidebar rotating network panel. Add a left sidebar to the profile page showing the profile owner's network. The sidebar has a header saying My Network with a small customize gear icon. The sidebar shows a rotating carousel of people — cycling through them every 4 seconds with a smooth fade transition. Each person card shows: their avatar with mood emoji overlaid, display name, current mood label, one line of their latest bulletin or status text, and a View Profile link. The rotation order and visible people are fully customizable — clicking the gear icon opens a Network Sidebar Settings panel where the profile owner can: see all eligible people (co-participants, circle members, vouched connections), drag to reorder them, toggle specific people to hide them from the sidebar, set how many people show at once (1 to 6), toggle the rotation on or off (some people may prefer a static display), choose the display style (cards, avatars only, or list). Save these settings in a profile_network_settings jsonb column on the profiles table. On mobile the sidebar becomes a horizontal scrolling strip at the top of the profile page above the board grid showing avatar bubbles with names. People who have no avatar show their initials in a colored circle using their accent color.
FEATURE FOUR — Profile stickers and embellishments. Add a sticker system to profile pages. In Edit Layout mode add a Stickers button that opens a sticker picker panel. Stickers are placed on the profile page as absolutely positioned elements on top of everything else. The sticker picker has four tabs: Emojis — a searchable grid of large decorative emojis organized by category (faces, nature, food, activities, objects, symbols, community). Must include 🦠 Shenaniganning tardigrade as a featured sticker in the community category. Community Badges — special badges earned or available on the platform: Founding Member hexagon, Verified checkmark, Covenant Agreed seal, Veteran badge, First Responder badge, Neighborhood Fave 2024 and 2025 badges (for HHR), custom text badges the user creates with their own text and chosen color. Custom Upload — user uploads their own image sticker from their device, stored in R2, max 500KB, displayed as a transparent PNG if uploaded as PNG. Text Stickers — add a text box anywhere on the profile with custom font, size, color, and one of the four available fonts (Classic, Modern, Typewriter, Editorial). Each placed sticker has: a drag handle to reposition anywhere on the profile, resize handles on corners, rotation handle to tilt it, a delete X button, and an opacity slider. Sticker positions are saved as a JSON array in a profile_stickers jsonb column: each entry has type, value, x percent, y percent, size, rotation, opacity. Stickers render on the profile page for all visitors. In Edit Layout mode stickers show their handles. In normal view they render cleanly with no handles. On mobile sticker editing uses touch drag and pinch to resize. Add a Clear All Stickers button in Edit Layout mode with confirmation.
FEATURE FIVE — Legislative tracking section. Add a new top level navigation item called Legislature. Build the following: A legislative dashboard with four sub-sections: Federal (Congress), State (Alabama Legislature), Local (Huntsville City Council and Madison County Commission), and Votes and Records. Connect to these free public APIs: Congress.gov API for federal bills and voting records, OpenStates API at openstates.org for Alabama state legislature data, ProPublica Congress API for voting records and campaign finance. Build a bills feed showing active legislation relevant to community issues — filter by topic tags: health, environment, housing, surveillance, parental rights, veterans, land use, education, civil liberties. Each bill card shows: bill number and title, plain English AI summary generated by the Anthropic API, current status (introduced, committee, floor vote, passed, signed, vetoed), last action date, how Alabama representatives voted if applicable, a Contact Your Rep button that opens a pre-filled customizable message template. Add a Representatives section showing profiles for Alabama's federal and state representatives: name, photo, party, district, current committees, recent votes on community-relevant bills, top campaign donors from OpenSecrets or FEC data, community responsiveness rating from verified members (1-5 stars with written comment, one rating per member per representative per 6 months). Add an Alerts system — users can subscribe to alerts for specific bills, topics, or representatives. When a bill they follow changes status or a representative they follow casts a vote they get a notification in the platform. Add a Community Action section — when a bill is in a critical window (committee hearing, floor vote scheduled, comment period open) show a Community Action Alert with the deadline, what action is needed, and a Contact Rep button. Add a Votes and Records archive where the community can document local government votes — city council, planning commission, school board — that don't have an API. Community members submit the vote record with source URL and the vote gets displayed on the relevant representative or body profile. Store legislative data in new tables: bills (id, source, bill_number, title, summary, ai_summary, status, topic_tags, last_action, created_at), representative_votes (id, bill_id, representative_id, vote, created_at), representatives (id, name, level, district, party, photo_url, created_at), community_ratings (id, user_id, representative_id, rating, comment, created_at), legislative_alerts (id, user_id, alert_type, target_id, created_at). Run the AI bill summarization using the existing Anthropic API key — summarize each new bill in plain English when it enters the system. Add the Legislature section to the mobile bottom tab bar replacing or alongside existing tabs. FEATURE SIX — Watch dashboard instructions. Add a collapsible How This Works panel at the top of every Watch dashboard tab. The panel is expanded by default for first-time visitors (tracked via localStorage per dashboard) and collapsed for returning visitors. Each dashboard has its own specific instructions covering: what this dashboard tracks, how to submit a report, what happens after submission including AI analysis and cross-referencing, how to interpret severity levels and classifications, and tips for making reports most useful including photo guidance GPS importance and source URLs. For the Atmospheric Observations dashboard specifically include instructions about the flight cross-reference system, how EXPLAINED versus UNEXPLAINED versus UNIDENTIFIED classifications work, the importance of GPS coordinates, and how to submit soil and rainwater lab results. For the Land Development dashboard include instructions about how to find and submit public records, what the AI pattern analysis looks for, and how to use the public records tracker. Style the panel with a light info background, a toggle arrow, and a Tips for best results section at the bottom of each one.
FEATURE SEVEN — Did You Know tip bar. Add a subtle rotating tip bar that appears at the bottom of the main feed page and profile pages. Tips fade in and out every 10 seconds. Add a permanent dismiss button (X) that hides the bar forever using localStorage. Tips to include: Did you know your profile page is fully customizable — click Edit Profile to get started. Did you know atmospheric observations are automatically cross-referenced with live flight data from the OpenSky Network. Did you know you can earn skill endorsements from community members on your Professional board. Did you know you can install Mycelium on your phone as an app — look for Add to Home Screen in your browser menu. Did you know the Watch dashboards use AI to detect patterns across community reports. Did you know you can create a Business Page and link it to your profile. Did you know founding members who were personally invited by AMBHaggermaker have a special hexagon badge. Did you know chat rooms under Circles and Homeschool Hub are separate from the main chat rooms. Did you know you can customize your profile background with photos or CSS patterns. Did you know the Lost and Found platform has an AI briefing feature for active search cases. Add ten more tips that make sense based on platform features. Style the tip bar as a subtle dark strip at the bottom with small text and the dismiss button on the right. On mobile it sits just above the bottom tab bar.
FEATURE EIGHT — Feedback and suggestion form. Add a floating feedback button — a speech bubble icon — in the bottom right corner of every page sitting above the mobile tab bar on mobile. Clicking it opens a modal form with fields: type dropdown (Bug Report, Feature Suggestion, Content Issue, General Feedback, Other), description text area (required, minimum 20 characters), optional screenshot upload stored in R2, and an anonymous submission toggle — when anonymous is checked the submission records that a verified member submitted it but does not store which member. Add a FEEDBACK_EMAIL environment variable — when set send a notification email via existing SMTP when feedback is submitted. Add a Feedback tab to the admin panel showing all submissions with: type badge, description, screenshot if attached, submitted by (or Anonymous), date submitted, and a status dropdown (New, Reviewing, In Progress, Completed, Wont Fix). Admins can add internal notes to each submission. When a non-anonymous submission is marked Completed or In Progress and the admin has added a note — send the submitter a notification that their feedback was reviewed with the admin note included. Style the floating button in the platform accent green with a white icon. On mobile make it large enough to tap easily — minimum 48x48px. Also add pattern backgrounds as an alternative to photo backgrounds. Add a Patterns tab in the background settings alongside the Photo tab. Pattern options: Solid color (default), Diagonal stripes, Horizontal stripes, Vertical stripes, Grid, Dots, Checkerboard, Zigzag, Diamonds, Honeycomb, Crosshatch, Waves, Triangles, Stars, Mycelium (organic branching network pattern — CSS generated). Each pattern has two color pickers: Primary color and Secondary color. Colors default to the user's accent color and a darker shade of it but can be set to anything. Add a pattern scale slider — small medium large — controlling the size of the pattern elements. Add a pattern opacity slider. Patterns are generated entirely in CSS using repeating-linear-gradient, repeating-radial-gradient, or SVG background patterns — no images needed, no R2 storage. Store pattern_type, pattern_color_primary, pattern_color_secondary, pattern_scale, and pattern_opacity in the profiles table alongside the photo background settings. The background picker shows a live preview of each pattern as you hover over it so people can see exactly what it looks like before selecting. On mobile the pattern picker shows a scrollable grid of previews. The Mycelium pattern specifically should look like branching mycelium threads — thin organic lines radiating outward in the accent color against a darker background. Then rebuild and redeploy and push to GitHub.